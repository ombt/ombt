#
# tests for groups
#
program
{
	#include "axioms/groups"
	#
	|- P(x, x) = 0;
	#
	|- P(P(x, x), x) = x;
	#
	|- P(x, y) = P(y, x);
	#
	|- P(P(x, y), I(x)) = P(y, y);
	#
	|- P(P(x, y), P(I(x), I(y))) = 0;
	#
}
