To create a TAR gzip file:

1) create a new directory and copy all the files which are to archive
into this directory.

2) cd to new directory.

3) to create tar/gzip file, enter:

	$ tar cf - . | gzip -c >/tmp/XXX.tar.gz

4) to expand file, enter:

	$ gunzip XXX.tar.gz
	$ tar xf XXX.tar



